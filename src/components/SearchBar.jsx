function SearchBar() {
    return (
        <section>
            <input type = "text" placeholder = "Search for a movie..." />
            <button> Search </button>
        </section>
    );
}

export default SearchBar;